version: 2
updates:
  - package-ecosystem: "github-actions"
    # The second entry covers the local composite action, whose pinned
    # r-lib and cache actions a "/" scan of .github/workflows never sees.
    directories:
      - "/"
      - "/.github/actions/*"
    schedule:
      interval: "weekly"
