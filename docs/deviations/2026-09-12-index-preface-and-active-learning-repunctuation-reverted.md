## 2026-09-12 — Revert the Preface and "Active learning" to Neave's own punctuation

- **What** — Reverted 16 punctuation changes in `index.qmd` that departed from the source, all confined to the Preface and the "Active learning" section:
  1. **Punctuation moved back outside the closing quotation mark** (3): `“distance-learning.”` → `“distance-learning”.`, `“prep,”` → `“prep”,`, `“active learning.”` → `“active learning”.`
  2. **Abbreviations re-pointed as Neave writes them** (5): `etc.` → `etc`, `i.e.,` → `i.e.`, `e.g.,` → `e.g.`, `homework, etc.,` → `homework etc,`, `it—i.e.,` → `it—i.e.`
  3. **Six inserted commas removed**: `Instead,` → `Instead`, `By the end of 2012,` → `2012`, `eight years,` → `years`, `Engineering,` → `Engineering` (an Oxford comma inside Neave's own list), `in active learning,` → `learning`, `On other occasions,` → `occasions`, `most of the time,` → `time`
  4. **A hyphen restored**: `subject matter` → `subject-matter`
  Also restored two lost italic runs on the course title (`*12 Days to Deming*`, `index.qmd:29` and `:37`) and two commas dropped from a parenthetical ("will grow to **value, and gain from,** that same wisdom", `:37`).
- **Where** — `index.qmd:19`, `:27`, `:29`, `:33`, `:37`, `:43`, `:45`, `:47`, `:49`, `:51`.
- **Source reference** — `A.PLEASE.START.HERE.08Feb22.pdf`, PDF pages 7–10 (printed Preface pp. 1–2 and Welcome pp. 1–2). Every change confirmed on the page images.
- **Why** — Epic [#734](https://github.com/lddurbin/twelve_days_to_deming/issues/734)'s Wave 2 pass over `index.qmd` ([#811](https://github.com/lddurbin/twelve_days_to_deming/issues/811), findings `I-01`, `I-03` to `I-07`, `I-09`, `I-10`) found that **this file's first two sections had been copy-edited and the other seven had not**. A word-level diff of Time management, Self-study, the "Overture", the Technophobes section, "Out-of-hours" work, *Twelve* Days? and The prescribed text against the source returns nothing but correct italics. `AGENTS.md` requires exact transcription, and the site's own corpus already follows Neave: `i.e.` appears 200 times against `i.e.,` 5, and `e.g.` 72 times against `e.g.,` 2 — **four of those seven corpus-wide outliers were in this one section**. So the re-punctuation departed from the source *and* from the site's practice everywhere else. This entry records the reversion because the changes were themselves an unlogged departure; it introduces no new deviation.
- **Decided in** — Wave 2 `index` adjudication pass, [#811](https://github.com/lddurbin/twelve_days_to_deming/issues/811) (2026-09-12): Lee accepted all eight findings.
- **Landed in** — PR [#814](https://github.com/lddurbin/twelve_days_to_deming/pull/814).
- **Related** — One further reversion of the same kind, outside these two sections, landed in the same pass: `index.qmd:302` had "inserted a couple of words into **a** statement from the narrator, Lloyd Dobyns, in the historically important TV documentary" where the source has "**the** statement" and punctuates the title with a colon (finding `I-23`, PDF page 19 / printed Welcome page 11). It is recorded here rather than as its own entry because it is the same act — restoring Neave's wording — and, like the changes above, it corrects an unlogged departure rather than creating one. It is worth noting how it was found: it sits inside the "Page references and call-outs" section, whose rewrite is documented by #212 / PR #245, and the pass initially cleared the whole section on that basis. The rewrite had in fact **preserved this sentence verbatim**, so the clear was drawn too wide; the post-fix validator re-run caught it at 98%, the highest-scoring flag left on the pass.
